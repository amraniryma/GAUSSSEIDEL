# Commit 3: Read matrix A
def read_matrix(n):
    A = np.zeros((n, n), dtype=float)  # create n x n zero matrix
    for i in range(n):
        for j in range(n):
            A[i][j] = read_float(f"A[{i+1}][{j+1}]: ")  # fill matrix
    return A
